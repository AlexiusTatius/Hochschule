1. For Frontend  (ReactJS): 
    - Go to the frontend folder
    - Run `npm run dev` to start the server
2. For Backend (NodeJS):
    - Go to the backend folder
    - Run `npm run dev` to start the server
